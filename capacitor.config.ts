import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speedvendors',
  appName: 'Speed Vendors',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;