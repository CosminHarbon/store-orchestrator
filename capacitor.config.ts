import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speedvendors.app',
  appName: 'Speed Vendors',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;