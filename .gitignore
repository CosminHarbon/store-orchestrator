# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (local, machine-specific config; use .env.example as template)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude worktrees (may contain their own .env) and downloaded CLI tooling/caches
.claude/
.tools/

# Cursor SDK storefront PoC: disposable per-merchant workspaces (developer-only, never commit)
.cursor-storefront-workspaces/
